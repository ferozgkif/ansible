---
- name: deployment
  hosts: node
  become: yes
  
  tasks:
  - name: install apache
    yum:
      name: httpd
      state: present
      #  dest:/var/www/html
      
  - name: start apache
    service:
      name: httpd
      state: started
